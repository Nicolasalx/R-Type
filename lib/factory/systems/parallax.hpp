/*
** EPITECH PROJECT, 2024
** R-Type
** File description:
** parallax
*/

#include "Registry.hpp"

/**
 * @brief Updates the parallax effect in the ECS registry.
 *
 * This function is responsible for updating the parallax effect within the
 * given registry. It processes all relevant entities and components to
 * achieve the desired parallax scrolling effect.
 *
 * @param reg Reference to the ECS registry.
 */
namespace ecs::systems {

void parallax(Registry &reg);

} // namespace ecs::systems
